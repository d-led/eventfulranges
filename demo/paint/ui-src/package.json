{
  "name": "eventfulranges-paint-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "format": "prettier --write .",
    "test": "playwright test",
    "test:unit": "vitest run",
    "bench": "node bench/spatial.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "esbuild": "^0.28.2",
    "prettier": "^3.9.6",
    "vitest": "^5.0.0"
  }
}
