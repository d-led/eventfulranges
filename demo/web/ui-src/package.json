{
  "name": "eventfulranges-web-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:local": "node build-local.mjs",
    "watch": "node build.mjs --watch",
    "serve:local": "node serve-local.mjs",
    "test": "playwright test",
    "test:local": "playwright test -c playwright.local.config.mjs",
    "test:unit": "node --test slice.unit.test.mjs camera.unit.test.mjs compaction.unit.test.mjs readout.unit.test.mjs backoff.unit.test.mjs local-engine.unit.test.mjs"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "esbuild": "^0.28.2"
  }
}
