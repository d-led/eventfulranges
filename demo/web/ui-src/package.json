{
  "name": "eventfulranges-web-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:local": "node build-local.mjs",
    "watch": "node build.mjs --watch",
    "test": "playwright test",
    "test:local": "playwright test -c playwright.local.config.mjs",
    "test:unit": "node --test slice.unit.test.mjs camera.unit.test.mjs backoff.unit.test.mjs"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "esbuild": "^0.28.2"
  }
}
